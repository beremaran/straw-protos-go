# Contributing to Straw Go bindings

Generated files are changed only by the tagged-source generation workflow. Hand-written validation and signing helpers
live beside, but separately from, generated output. Run `make check` before opening a pull request and update tests for
behavior changes. Report vulnerabilities through [SECURITY.md](SECURITY.md), not an issue.
